## 1. Add Admin Newsletter Subscribers Endpoint

- [x] 1.1 Add `admin_subscribers` handler function in `src/api.rs` that calls `news::get_subscriptions` and returns JSON
- [x] 1.2 Register the new route `GET /api/admin/news/subscribers` in the admin router group in `src/api.rs`

